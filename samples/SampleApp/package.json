{
  "name": "sampleapp-views",
  "private": true,
  "description": "Build toolchain for the JsxCore sample app: the TypeScript compiler and esbuild. JsxCore invokes both native binaries directly; Node itself is never required.",
  "devDependencies": {
    "typescript": "^7.0.2",
    "esbuild": "^0.28.1"
  },
  "dependencies": {
    "marked": "^18.0.7",
    "preact": "^10.29.7",
    "preact-render-to-string": "^6.7.0"
  }
}
