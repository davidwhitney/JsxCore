{
  "name": "sampleapp-views",
  "private": true,
  "description": "TypeScript toolchain for the JsxCore sample app. JsxCore invokes the native tsc binary from this package directly; Node itself is never required at runtime.",
  "devDependencies": {
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "marked": "^18.0.7",
    "preact": "^10.29.7",
    "preact-render-to-string": "^6.7.0"
  }
}
