// Deliberately outside any namespace: these types belong in the root generated module.

/// <summary>A model with no .NET namespace.</summary>
internal sealed record GlobalNamespaceModel(string Name);
